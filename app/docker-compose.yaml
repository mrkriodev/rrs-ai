version: "3"

services:
  api:
    build: ./app
    container_name: fastapi-ml
    environment:
      - MODEL_PATH=/
    ports:
      - "8000:8000"
    networks:
      - network

networks:
    network: {}